<%
  const serverURL = ''
  const ENVType = process.env.NODE_ENV
  rootURL = ENVType === 'production' ? serverURL : ''
%>

<%- include('./_modules/_contents.ejs') %>

<% if(ENVType === 'development') {-%>
<%- include('./_modules/_footer.ejs') %>
<%} -%>
